pre-commit:
  parallel: true
  commands:
    api-check:
      root: backend/
      run: npx biome check src/

pre-push:
  parallel: true
  commands:
    api-test:
      root: backend/
      run: npx vitest run
      stage_fixed: true
